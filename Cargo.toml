[package]
name = "htmx"
version = "0.1.0"
edition = "2024"

[dependencies]
axum = { version = "0.8.8", features = ["tokio", "json", "macros", "ws"] }
serde = { version = "1.0.228", features = ["derive"] }
tokio = { version = "1.49", features = ["rt-multi-thread", "sync"] }
tower-http = "0.6.8"
